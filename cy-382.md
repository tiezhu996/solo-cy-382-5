请根据以下要求生成项目代码和 README。

## 项目主要需求

### 项目概述
**项目类型：全栈Web应用**
搭建一个旅伴匹配与行程共享平台，帮助有相同旅行计划的用户找到旅伴、共享行程信息，解决独自出行缺乏同伴的痛点。

### 功能需求

1. **行程发布**：用户可发布旅行计划，包含目的地、出发时间、行程天数、预算范围、出行方式（自驾/公共交通/徒步）、期望旅伴人数和性别偏好。
2. **旅伴匹配**：系统根据目的地、时间、预算等条件智能推荐匹配的行程，用户可浏览并申请加入他人行程。
3. **行程协作看板**：旅伴确认后进入协作空间，支持共享每日行程安排、住宿信息、交通方案，所有成员可编辑。
4. **费用预算管理**：行程内设置总预算和各项费用（交通/住宿/餐饮/门票），实时显示已花费与剩余预算。
5. **即时沟通**：行程成员间内置文字聊天，支持发送位置、图片，方便旅途中实时协调。
6. **旅行日记**：行程结束后成员可共同撰写旅行日记，上传照片，生成可分享的旅行报告。
7. **用户主页**：展示个人旅行经历、去过的目的地地图、收到的旅伴评价，建立旅行信用体系。

### 技术要求

- 前端：React 18 + TypeScript + Ant Design，构建工具 Vite
- 后端：NestJS + TypeScript，ORM 使用 TypeORM
- 数据库：MySQL 8.0
- 认证：JWT
- 实时通信：Socket.IO（聊天功能）
- 地图：高德地图 JS API（目的地地图展示）
- 代码规范：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、状态枚举、配置项）统一维护在后端 `backend/src/constants/` 目录下；日志统一使用 NestJS 内置 Logger，日志格式配置在 `backend/src/common/logger/`；异常信息统一定义在 `backend/src/common/errors/`，通过 NestJS ExceptionFilter 统一捕获并返回标准格式响应。

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面，组件，路由，状态管理，API 请求，类型定义，样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

后端目录结构建议：
```
backend/
├── src/
│   ├── modules/
│   │   ├── user/           # 用户模块
│   │   ├── trip/           # 行程模块
│   │   ├── companion/      # 旅伴匹配模块
│   │   ├── chat/           # 聊天模块
│   │   └── diary/          # 旅行日记模块
│   ├── common/
│   │   ├── guards/         # 认证守卫
│   │   ├── filters/        # 异常过滤器
│   │   ├── interceptors/   # 日志拦截器
│   │   ├── errors/         # 统一错误定义
│   │   └── logger/         # 日志配置
│   ├── config/             # 数据库、环境变量配置
│   └── main.ts
├── database/
│   └── init.sql
└── package.json
```

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=tripmatch`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理 + WebSocket Upgrade
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: tripmatch`
- 所有服务使用 `container_name`，容器名带 `tripmatch-` 前缀
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API（`/api` → `backend:3000`），WebSocket 需配置 Upgrade/Connection 头
- 数据库配置 `healthcheck`，后端 `depends_on` 数据库健康
- 端口映射：
  - 前端：18402:80
  - 后端：19402:3000

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址（前端：http://localhost:18402）
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

要求：Docker 部署命令放在最前面；命令可直接复制运行；目录结构用代码块展示；技术栈用表格展示；告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   node_modules/
   dist/
   .env
   .DS_Store
   *.log
   ```
3. 执行 `git add -A && git commit -m "init: 项目初始化"`
